{
  "name": "nummus-nav-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Official NUMMUS NAV and Treasury Backing dashboard.",
  "type": "module",
  "scripts": {
    "collect": "tsx collector/collectDailySnapshot.ts",
    "import:early-vault": "tsx collector/importEarlyVaultHistory.ts",
    "import:vault-history": "tsx collector/importVaultHistory.ts",
    "check": "tsc --noEmit",
    "dev": "VITE_BASE_PATH=/ vite --host 127.0.0.1",
    "build": "vite build && node scripts/copy-data.mjs",
    "build:local": "VITE_BASE_PATH=/ vite build && node scripts/copy-data.mjs",
    "preview:local": "VITE_BASE_PATH=/ vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@solana/spl-governance": "^0.3.28",
    "@solana/web3.js": "^1.98.4",
    "vite": "^5.4.19"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  }
}
